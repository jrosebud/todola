<h1>Welcome to ToDo L.A.!</h1>

<h2><em>Your bossy friend who always has a suggestion.</em></h2>

<h3>What do you want to do?</h3>

<%= render "searchform" %>

<hr />

<h3>Here's what's happening now!</h3>

<div class="place-grid">
<% @places.time_range.each do |p| %> 
  <div class="span3 place-listing"> 
  <%= link_to (image_tag (p.image.thumb.url)), place_path(p) %>
  <h4><%= link_to p.name, place_path(p) %></h4>
  </div>
<% end %>
</div>

<hr />

<h3>Or go out and explore these places!</h3>

<%= render "grid" %>

<div style="clear: both;"></div>

<% if user_signed_in? %>
<hr />
<p><%= link_to "Create a new listing", new_place_path %></p>
<% end %>